// src/config/database.d.ts

export declare module '../config/database.js' {
    import { Pool } from 'mysql2/promise'; // Importa o tipo Pool de mysql2/promise
    const pool: Pool;
    export default pool;
  }
  
  